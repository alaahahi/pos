# القيم المطلوبة في ملف .env

## 📝 قم بإضافة هذه القيم إلى ملف `.env` الخاص بك

```env
# ============================================
# إعدادات نظام الترخيص (License System)
# ============================================

# تفعيل/تعطيل نظام الترخيص
# true = مفعل | false = معطل
LICENSE_ENABLED=true

# التحقق من الترخيص عند كل طلب
# false = أفضل للأداء (يوصى به)
# true = للتحقق المستمر (قد يؤثر على الأداء)
LICENSE_CHECK_EVERY_REQUEST=false

# فترة السماح بعد انتهاء الترخيص (بالأيام)
# يمكن للمستخدم الاستمرار في الاستخدام لمدة 7 أيام بعد انتهاء الترخيص
LICENSE_GRACE_PERIOD=7

# فترة التحقق الدوري من الترخيص (بالثواني)
# 3600 = ساعة واحدة
# 86400 = يوم واحد
LICENSE_VERIFICATION_INTERVAL=3600

# دعم التفعيل Offline
# true = يمكن تفعيل الترخيص بدون اتصال بالإنترنت
# false = يتطلب اتصال بالإنترنت للتفعيل
LICENSE_OFFLINE_MODE=true

# Secret Key لتوقيع الترخيص
# ⚠️ مهم جداً: يجب تغيير هذا المفتاح إلى قيمة عشوائية قوية
# استخدم الأمر التالي لتوليد مفتاح آمن:
# php -r "echo bin2hex(random_bytes(32));"
LICENSE_SECRET_KEY=your-secret-key-change-this-to-random-string

# URL للتحقق Online من الترخيص (اختياري)
# اتركه فارغاً للعمل Offline فقط
# مثال: https://your-license-server.com/api/verify
LICENSE_VERIFICATION_URL=

# السماح بتعدد التثبيتات لنفس المفتاح
# false = لا يسمح بتثبيت نفس المفتاح على أكثر من سيرفر
# true = يسمح بتثبيت نفس المفتاح على عدة سيرفرات
LICENSE_ALLOW_MULTIPLE=false

# ============================================
# إعدادات المزامنة (Sync Settings)
# ============================================

# مسار ملف SQLite للمزامنة
# سيتم استخدامه لتخزين البيانات محلياً للمزامنة
# المسار النسبي من مجلد المشروع
SYNC_SQLITE_PATH=database/sync.sqlite
```

---

## 🔑 توليد Secret Key آمن

### الطريقة 1: استخدام PHP
```bash
php -r "echo bin2hex(random_bytes(32));"
```

### الطريقة 2: استخدام Laravel
```bash
php artisan key:generate --show
```

### الطريقة 3: استخدام OpenSSL
```bash
openssl rand -hex 32
```

---

## ✅ مثال على ملف .env كامل

```env
APP_NAME=POS
APP_ENV=local
APP_KEY=base64:your-app-key-here
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pos
DB_USERNAME=root
DB_PASSWORD=

# ============================================
# إعدادات نظام الترخيص
# ============================================
LICENSE_ENABLED=true
LICENSE_CHECK_EVERY_REQUEST=false
LICENSE_GRACE_PERIOD=7
LICENSE_VERIFICATION_INTERVAL=3600
LICENSE_OFFLINE_MODE=true
LICENSE_SECRET_KEY=your-secret-key-change-this-to-random-string
LICENSE_VERIFICATION_URL=
LICENSE_ALLOW_MULTIPLE=false

# ============================================
# إعدادات المزامنة
# ============================================
SYNC_SQLITE_PATH=database/sync.sqlite
```

---

## 📌 ملاحظات مهمة

1. **LICENSE_SECRET_KEY**: ⚠️ يجب تغيير هذا المفتاح قبل الإنتاج. استخدم أحد الطرق أعلاه لتوليد مفتاح آمن.

2. **LICENSE_ENABLED**: إذا كنت تريد تعطيل نظام الترخيص مؤقتاً، ضع `false`.

3. **LICENSE_OFFLINE_MODE**: إذا كان `true`، يمكن تفعيل الترخيص بدون اتصال بالإنترنت.

4. **SYNC_SQLITE_PATH**: المسار النسبي من مجلد المشروع. سيتم إنشاء الملف تلقائياً عند الاستخدام.

5. بعد إضافة القيم، قم بتشغيل:
   ```bash
   php artisan config:clear
   php artisan cache:clear
   ```




